services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: platzwart
      POSTGRES_USER: platzwart
      POSTGRES_PASSWORD: platzwart_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
